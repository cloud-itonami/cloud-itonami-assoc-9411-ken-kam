;; The KAM source catalog, as Kotoba.
;;
;; GENERATED from data/datascript-tx.edn, which src/association/facts.cljc
;; reads as data. Both are the same catalog; this one reaches the Kotoba
;; oracle, wasm and both native ISAs, which the .cljc cannot.
;;
;; `:topic` is a SET in the source. A set has no order and `topic` is
;; indexed by position, so the generator uses the order the data file
;; writes. `by-topic-id` answers by name and is unaffected.
;;
;; Parity: test/association_facts_kotoba_parity_test.clj compares every
;; field of every entry against the .cljc, so this file cannot drift from
;; the data without the suite saying so.

(ns association-facts
  (:export [main association-covered? entry-count entry-field topic-count
            topic by-topic-count by-topic-id coverage-note]))

(defn association-covered? [a :string] :bool (string=? a "kam"))

(defn entry-count [a :string] :i64 (if (association-covered? a) 2 0))

(defn valid-entry? [a :string i :i64] :bool
  (if (association-covered? a) (if (< i 0) false (< i 2)) false))

(defn entry-field [a :string i :i64 f :string] [:option :string]
  (if (valid-entry? a i)
    (if (= i 0)
      (cond
        (string=? f "id") (option-some-of [:option :string] "kam.founding-1959")
        (string=? f "title") (option-some-of [:option :string] "Kenya Association of Manufacturers (KAM) established in 1959, per KAM's own Chairman quoted directly in Manufacturing Outlook's spotlight article, corroborated by LinkedIn's official 'Founded: 1959' company profile field")
        (string=? f "association") (option-some-of [:option :string] "kam")
        (string=? f "isic") (option-some-of [:option :string] "9411")
        (string=? f "country") (option-some-of [:option :string] "KEN")
        (string=? f "kind") (option-some-of [:option :string] "governance-program")
        (string=? f "url") (option-some-of [:option :string] "https://www.mfg-outlook.com/manufacturing-spotlights/kenya-association-of-manufacturers-kam-spotlight")
        (string=? f "url-provenance") (option-some-of [:option :string] "mfg-outlook-corroborated")
        (string=? f "established-date") (option-some-of [:option :string] "1959")
        (string=? f "retrieved-at") (option-some-of [:option :string] "2026-07-17")
        :else (option-none-of [:option :string])
        )
      (cond
        (string=? f "id") (option-some-of [:option :string] "kam.golden-jubilee-rebrand-2009")
        (string=? f "title") (option-some-of [:option :string] "KAM unveiled a new corporate image in 2009, marking 50 years since its 1959 establishment (WebSearch-corroborated across multiple independent results, zero conflicting alternative date; kam.co.ke itself returned a TLS certificate-verification error on every page tried this tick)")
        (string=? f "association") (option-some-of [:option :string] "kam")
        (string=? f "isic") (option-some-of [:option :string] "9411")
        (string=? f "country") (option-some-of [:option :string] "KEN")
        (string=? f "kind") (option-some-of [:option :string] "governance-program")
        (string=? f "url") (option-some-of [:option :string] "https://www.mfg-outlook.com/manufacturing-spotlights/kenya-association-of-manufacturers-kam-spotlight")
        (string=? f "url-provenance") (option-some-of [:option :string] "websearch-corroborated")
        (string=? f "established-date") (option-some-of [:option :string] "2009")
        (string=? f "retrieved-at") (option-some-of [:option :string] "2026-07-17")
        :else (option-none-of [:option :string])
        )
      )
    (option-none-of [:option :string])))

(defn topic-count [a :string i :i64] :i64
  (if (valid-entry? a i)
    (if (= i 0) 1 (if (= i 1) 1 0))
    0))

(defn topic [a :string i :i64 t :i64] [:option :string]
  (if (valid-entry? a i)
    (if (= i 0)
      (if (= t 0) (option-some-of [:option :string] "governance") (option-none-of [:option :string]))
    (if (= i 1)
      (if (= t 0) (option-some-of [:option :string] "governance") (option-none-of [:option :string]))
    (option-none-of [:option :string])))
    (option-none-of [:option :string])))

(defn by-topic-count [a :string candidate :string] :i64
  (if (association-covered? a)
    (if (string=? candidate "governance") 2 0)
    0))

(defn by-topic-id [a :string candidate :string i :i64] [:option :string]
  (if (association-covered? a)
    (if (= i 0)
      (if (string=? candidate "governance") (entry-field a 0 "id") (option-none-of [:option :string]))
      (option-none-of [:option :string]))
    (option-none-of [:option :string])))

(defn coverage-note [a :string] [:option :string]
  (if (association-covered? a)
    (option-some-of [:option :string]
      "cloud-itonami-assoc-9411-ken-kam Wave 0 (ADR-2607141700): 2 kam entries seeded with an official citation. Extend the sovereign catalog; never fabricate a rule id/url.")
    (option-none-of [:option :string])))

(defn main [] :i64 0)
